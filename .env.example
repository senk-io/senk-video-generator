# Seedance / BytePlus ModelArk
# Write values only to a local .env. Do not commit real secrets.
ARK_API_KEY=
# MiniMax H3 Open Platform V2 API key. Keep the real value in an untracked .env or the current shell only.
MINIMAX_API_KEY=

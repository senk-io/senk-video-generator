# AI Native Engineering 协作协议

## 1. 适用范围

本文件是本目录及其全部子目录的根级协作协议。任何在本项目中执行规划、设计、编码、生成、验证、修复或文档维护的 Codex，都必须先遵守本文件。

若指令冲突，按以下顺序处理：

1. 当前用户明确指令。
2. 本文件。
3. `foundation/` 中已经正式冻结的基础制度。
4. `execution/` 与 `verification/` 中的执行及验收规则。
5. `video/` 中的领域规则。
6. `knowledge/` 中的经验资料。
7. 临时方案、历史讨论与实现细节。

历史对话只能作为设计来源和背景证据，不能替代已经落盘的制度与事实。

外部对话、模型回复和既有讨论均不构成审查权威，也不是制度推进的前置依赖。Codex 必须依据本地事实、冻结制度、可追踪证据和逻辑一致性独立提出、审查、接受或推翻草案；不得因为某项观点来自 ChatGPT、其他模型、既有作者或 Codex 自身就默认其正确。

Codex 可以在用户授权范围内作出草案审查结论和冻结准备度判断。正式冻结仍必须满足 `IF-0007` 的成立条件，并明确提案者、审查者、冻结权威、证据和版本边界；不得用一次模型判断冒充制度冻结。

标记为 `DRAFT`、`PROPOSAL` 或位于 `foundation/revisions/` 的文档没有制度权威，不能覆盖冻结制度，也不能作为运行时状态迁移依据。

## 2. 项目第一原则

本项目不是单纯追求“一次生成好视频”。本项目的目标是：

> 持续缩小创作预期与实际结果之间的可解释偏差。

所有设计和执行必须围绕以下闭环：

```text
Expectation
  -> Planning
  -> Execution
  -> Verification
  -> Gap
  -> Diagnosis
  -> Policy
  -> Next Execution
```

最重要的产物不只是视频结果，还包括：

- 明确的创作意图；
- 可执行的约束；
- 可观察的验证对象；
- 可复现的执行记录；
- 可解释的偏差；
- 有依据的下一步策略。

## 3. 不可违反的治理原则

### 3.1 预期优先

执行前必须先定义成功条件。无法定义的预期必须继续拆分，不能直接交给模型自由猜测。

```text
Intent -> Constraint -> Observable -> Verification
```

### 3.2 模型负责生成，系统负责治理

Seedance、Veo、Kling、Runway 或未来模型都只是能力提供者。项目制度、事实模型和流程不得绑定单一模型。

应定义 `Shot`、`GenerationTask`、`Asset` 和 `ProviderAdapter`，不得把核心概念写成某个模型专属对象。

### 3.3 生成不等于验收

以下事实必须分离：

```text
生成完成 != 技术合格
技术合格 != 语义符合
语义符合 != 正式选中
正式选中 != 已绑定时间线
已绑定时间线 != 允许发布
```

任何步骤不得用最终文件存在、接口成功或模型返回成功来代替正式验收。

### 3.4 单一事实来源

每类正式事实必须只有一个权威来源。缓存、界面、报表、索引、时间线预览和导出文件都是派生读面，不能反向篡改权威事实。

派生数据必须能够由权威事实重建。

### 3.5 权限与状态迁移

Agent 只能执行其被授权的状态迁移。没有明确授权、前置状态或证据时，不得自行宣布选择、批准、发布或完成。

重要状态必须通过显式事件或决策产生，不能靠覆盖字段伪造历史。

### 3.6 证据优先

任何 `PASS`、`FAIL`、选中、拒绝、失效、替换和发布结论都必须能回答：

- 谁或什么规则作出了判断；
- 判断针对哪个版本；
- 使用了哪些输入和约束；
- 证据保存在哪里；
- 判断发生于何时；
- 为什么得到这个结论。

没有证据的结论不得升级为正式事实。证据必须不可变、绑定适用版本、只属于一个现实并可重新审计；证据不足时必须保持 `UNKNOWN`，不得补推。

### 3.7 失败关闭

无法验证、证据缺失、状态不一致或依赖不明时，默认阻断后续高风险状态迁移。不得把未知当作通过。

### 3.8 历史不可伪造

修复错误应追加新的事实、决策或版本，并保留原始记录。除非用户明确要求清理可丢弃数据，不得通过覆盖或删除来制造“从未失败”的假象。

### 3.9 制度通过治理演化

运行时执行、偏差、诊断、策略、学习和知识不得直接修改冻结制度。发现制度冲突时，应建立修订提案并进入制度审查；提案在正式冻结前不得执行。

基础制度必须跨领域、独立于具体提供者。只能适用于视频的规则进入 `video/`，只能适用于具体模型的经验进入 `knowledge/`。

## 4. 必读制度及职责边界

开始实质工作前，按任务相关性阅读以下文件：

1. `foundation/00_ProjectVision.md`：项目为什么存在、解决什么问题。
2. `foundation/01_FirstPrinciples.md`：整个系统相信什么。
3. `foundation/02_Governance.md`：全局权威、状态、证据与失败处理制度。
4. `foundation/03_Architecture.md`：系统的概念架构。
5. `foundation/04_Glossary.md`：全仓库统一术语。
6. `foundation/05_Evidence.md`：正式证据、信任根和审计边界。
7. `foundation/06_Institution.md`：制度资格、冻结、升级和架构边界。
8. `execution/Expectation.md`：如何把预期工程化。
9. `execution/Planning.md`：如何从意图形成计划。
10. `execution/Execution.md`：标准执行契约和 Agent 职责。
11. `execution/Verification.md`：执行阶段如何验证。
12. `execution/Gap.md`：如何表达预期与观察之间的偏差。
13. `execution/Diagnosis.md`：如何分类和解释偏差原因。
14. `execution/Policy.md`：诊断后允许采取的策略。
15. `execution/Learning.md`：如何把执行经验沉淀为知识。
16. 与当前任务相关的 `verification/` 检查定义。
17. 与当前任务相关的 `video/` 领域模型。
18. 必要时再读取 `knowledge/` 中的提供者和经验资料。

若文件为空、缺失或彼此冲突：

- 不得虚构其中已经存在的规定；
- 先以本文件为最低治理基线；
- 在结果中明确指出制度缺口；
- 若任务包含制度建设，再补齐对应文档；
- 若任务不包含制度建设，只做范围内工作，不擅自扩张任务。

`foundation/` 描述跨领域的不变量；`execution/` 描述 AI 的工作闭环；`video/` 只描述视频领域知识；`verification/` 保存检查定义；`knowledge/` 保存可变化经验。不得把模型经验写成制度，不得把视频规则提升为全局原则，也不得把状态权威、证据和验收制度降级为局部领域约定。

制度文件由用户分阶段组织与补全。除非用户明确要求，不得为了追求目录完整而一次性补写所有正文、字段契约或项目模板。

## 5. 预期模型

每项创作预期必须归入以下一类：

### 5.1 硬预期

必须满足且原则上可确定性验证，例如时长、画幅、指定角色、字幕安全区、必需台词、镜头方向和必需场景。

硬预期与观察不一致时必须建立独立偏差，并由后续权威进入失败关闭；不得用软评分抵消或由验证器直接输出 `FAIL`。

### 5.2 软预期

允许程度差异并可评分，例如节奏快慢、色调冷暖、镜头稳定性、主体位置和音乐克制度。

软预期必须记录评分尺度、阈值、置信度和证据，不能只给无依据的总分。

### 5.3 创意预期

依赖人类审美或受众感受，例如孤独感、希望感、电影感和观看欲望。

系统不得宣称自动“验证艺术”。应验证 AI 是否忠实执行了已表达的创作意图，并把不可客观裁决的部分交给人工评审。

模糊词不能直接作为唯一验收条件。例如“电影感”应拆为镜头运动、构图、光照、色彩、节奏、声音和情绪等可执行维度；仍不可量化的部分保留为创意评审项。

## 6. 标准执行协议

每次任务至少执行以下步骤：

1. **读取事实**：检查现有制度、代码、资产、状态和用户改动，不凭记忆假设。
2. **定义预期**：明确目标、范围、硬约束、软目标、创意目标和非目标。
3. **建立验证**：在执行前定义观察对象、证据和偏差成立条件，并明确哪个下游权威可以作出接受、失败或人工评审裁决。
4. **制定计划**：把工作拆成可验证、可回滚或可隔离的小步骤。
5. **执行最小变更**：只改变达成当前目标所必需的对象，保留用户已有工作。
6. **收集证据**：保存命令、输入版本、输出标识、检查结果、评分或人工决策。
7. **识别偏差**：不能只说“效果不好”或“失败”，必须描述预期与结果具体差在哪里。
8. **诊断原因**：至少判断偏差来自预期、约束、规划、提示、模型、资产、时间线、依赖、验证器或基础设施中的哪一类。
9. **应用策略**：依据诊断选择重试、修复提示、局部重生成、重新规划、替换提供者、降级、阻断或人工评审。
10. **复验与交付**：策略执行后重新验证；只有满足完成条件才可宣布完成。

不得在没有诊断的情况下盲目重复生成。重试只适用于瞬时故障或有证据表明随机采样可能解决的问题。

## 7. 验证输出与裁决状态

验证器的合法输出是可追踪观察，以及观察与冻结预期不同时形成的独立偏差。验证器不得直接产生 `PASS` 或 `FAIL`。

以下状态只能由策略或拥有相应权威的裁决者产生：

- `PASS`：裁决者确认适用偏差不阻断接受。
- `FAIL`：裁决者确认当前策略不能继续。
- `REQUIRES_REVIEW`：需要授权人员作创意或业务裁决。
- `UNKNOWN`：缺少观察、证据或裁决能力。

`UNKNOWN` 不得视为 `PASS`。`REQUIRES_REVIEW` 不得由 Agent 擅自改为 `PASS`，除非 Agent 被明确授予该裁决权并记录依据。

验证、诊断和策略必须分离：验证建立观察与偏差，诊断解释偏差，策略决定下一步。任何一层都不得为了让流程继续而修改上一层事实。

## 8. 偏差诊断与策略选择

诊断必须以偏差为唯一合法输入，提出受证据约束的原因解释，并记录置信度、未知项和受影响范围。诊断不是事实，允许多个候选解释并存：

```text
Expectation
Constraint
Planning
Prompt
Provider
ModelCapability
Asset
Timeline
Dependency
Verifier
Infrastructure
```

诊断不得包含重试、修复、升级或人工评审行动，也不得直接决定责任归属。策略应在独立阶段依据偏差和当前诊断选择行动：

- 瞬时基础设施故障：有限重试；
- 提示表达偏差：修复提示并保留前后版本；
- 规划或镜头拆分错误：重新规划受影响部分；
- 单一资产失败：局部重生成，不重做无关镜头；
- 模型能力不匹配：切换或组合能力提供者；
- 预期不可验证：补充可观察项或转人工评审；
- 上游事实变化：沿依赖图传播失效并重建派生产物；
- 权威冲突或证据缺失：阻断并报告。

只有策略有权授权未来执行。每次行动必须记录策略选择者、选择原因、所依据的偏差与诊断、策略版本、所需权威、执行意图和预算。执行器不得自行重试，策略不得修改历史，也不得通过接受偏差来隐藏偏差。

## 9. 模型中立与中间表示

创作意图应先进入项目自己的中间表示，再由适配器编译为各模型输入：

```text
CreativeIntent
  -> ShotSpec
  -> ProviderAdapter
  -> ProviderRequest
  -> CandidateAsset
```

提供者专属参数必须隔离在适配器边界内。不得让某个模型的提示格式、任务状态或返回结构成为项目权威模型。

更换提供者后，原始意图、镜头规格、候选资产、裁决和证据仍应可理解、可比较、可审计。

## 10. 资产、选择与依赖

所有生成结果首先登记为候选资产。候选资产必须保留来源、输入版本、模型与参数、生成任务、时间、成本以及适用限制。

资产进入正式时间线前，至少经过：

```text
GenerationTask completed
  -> CandidateAsset registered
  -> TechnicalValidation completed
  -> SemanticReview completed
  -> SelectionDecision authorized
  -> TimelineBinding created
```

脚本、角色设定、场景设定、镜头规格或上游资产变化时，必须计算受影响对象并传播失效。不得默认所有旧产物继续有效，也不得无差别重生成整个项目。

## 11. 文档与实现维护规则

- 新规则先判断属于全局治理、执行、验证、策略、架构还是视频领域，再写入对应文档。
- 不在多个文件复制同一条权威规则；其他文件通过引用连接。
- 制度变更必须说明动机、影响对象、迁移方式和验证方法。
- 实现若暂时无法满足制度，必须显式记录差距，不得悄悄降低制度。
- 代码、配置、数据结构和界面名称应与权威概念保持一致。
- 新增状态或状态迁移时，必须同时定义前置条件、授权者、证据和失败行为。
- 新增模型接入时，只增加适配器与能力声明，不修改领域事实语义来迎合模型。

## 12. 完成定义

任务只有同时满足以下条件才能标记完成：

- 用户要求的产物已经实际生成或修改；
- 所有适用硬预期均有符合预期的观察，且不存在未处理的硬偏差；
- 软预期已有评分或明确说明；
- 创意预期已获得人工裁决，或明确标记为 `REQUIRES_REVIEW`；
- 验证证据可定位；
- 未解决偏差、未知项和制度缺口已明确披露；
- 没有用派生结果替代权威事实；
- 没有破坏用户无关改动；
- 交付说明能够让下一位 Codex 从当前事实继续工作。

仅完成代码编写、模型调用成功、文件存在或测试命令启动，不等于任务完成。

## 13. 明确禁止

- 禁止把模糊审美词直接当作可自动验收的硬条件。
- 禁止把模型响应成功当作视频合格。
- 禁止跳过验证直接发布或绑定正式时间线。
- 禁止把 `UNKNOWN`、低置信度评分或缺失证据包装为成功。
- 禁止无诊断地无限重试或整项目重生成。
- 禁止为了通过检查而临时降低阈值、删除失败证据或修改历史。
- 禁止让某个提供者成为不可替换的核心事实来源。
- 禁止将领域知识与全局治理规则混在同一层级。
- 禁止在未读取现有文件和状态前大范围改写。

## 14. 协作与交付语言

面向用户的说明使用中文；代码、协议字段、命令、文件名和行业固有标识可保留英文。

交付时先报告真实结果，再报告验证证据、剩余偏差和需要人工裁决的事项。不要只陈述执行过程，也不要用笼统的“已优化”“效果很好”替代可核验事实。
